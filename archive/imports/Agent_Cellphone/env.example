# GitHub auth
GITHUB_USERNAME=
GITHUB_TOKEN=
DISCORD_WEBHOOK_URL=
DISCORD_CHANNEL_ID=
DEVLOG_USERNAME=
# Force non-interactive git (prevents popups)
GIT_TERMINAL_PROMPT=0
GCM_INTERACTIVE=Never

# Optional: verbose HTTP logging from git
GIT_CURL_VERBOSE=1

# =============================================================================
# SYSTEM PATH CONFIGURATION
# =============================================================================
# Main repository root (e.g., D:/repos, C:/projects, /home/user/projects)
# This is where all your repositories and projects are stored
REPOS_ROOT=D:/repos

# Clone destination used by scripts (legacy, kept for compatibility)
REPO_DEST=D:/repos

# Default organization/owner name (e.g., "Dadudekc", "YourOrg", "CompanyName")
# This determines the default subdirectory structure under REPOS_ROOT
DEFAULT_OWNER=Dadudekc

# Agent workspaces root (optional, defaults to REPOS_ROOT/DEFAULT_OWNER)
# AGENT_WORKSPACES_ROOT=D:/repos/Dadudekc

# Communications root (optional, defaults to REPOS_ROOT/communications)
# COMMUNICATIONS_ROOT=D:/repos/communications

# =============================================================================
# LEGACY COMPATIBILITY
# =============================================================================
# Legacy variable for backward compatibility
AGENT_FILE_ROOT=D:/repos/Dadudekc

# Notes:
# - Set these for your current PowerShell session using $env:VAR='value'
# - Example:
#     $env:REPOS_ROOT='C:/projects'
#     $env:DEFAULT_OWNER='YourCompany'
#     $env:GITHUB_USERNAME='dadudekc'
#     $env:GITHUB_TOKEN='<YOUR_PAT>'
#     $env:GIT_TERMINAL_PROMPT='0'
#     $env:GCM_INTERACTIVE='Never'
#
# - To use a different project focus:
#     $env:REPOS_ROOT='C:/my-projects'
#     $env:DEFAULT_OWNER='MyCompany'
#
# - To use a different communications location:
#     $env:COMMUNICATIONS_ROOT='C:/my-projects/agent-comms'
#
# - To use a different agent workspaces location:
#     $env:AGENT_WORKSPACES_ROOT='C:/my-projects/agent-workspaces'

