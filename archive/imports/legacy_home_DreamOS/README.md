# Legacy ~/DreamOS Import

This archive records the pre-unification `~/DreamOS` folder.

## Status

- Source: `~/DreamOS`
- Import type: legacy runtime/prototype archive
- Runtime status: non-active
- Promotion rule: inspect first, test before promotion

## Guardrails

- Do not activate old runtime state directly.
- Do not overwrite canonical DreamOS files.
- Do not import stale run artifacts as active state.
- Promote only bounded runtime primitives.
